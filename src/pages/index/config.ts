import { MAINHOST } from '../../config'

export default {
  getRecordData: {
    host: MAINHOST,
    url: '/api/recordDataApi'
  },
  getSpecialBook :{
    host: MAINHOST,
    url: '/api/getSpecialBook'
  }
}
